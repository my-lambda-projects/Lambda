'use strict';
$ (function () {
  $ ('a').click (function () {
    var a = $ ('p').css ('font-size'), b = parseFloat (a, 10);
    a = a.slice (-2);
    $ ('p').css ('font-size', b / 1.2 + a);
    'larger' == this.id && $ ('p').css ('font-size', 1.2 * b + a);
  });
});
