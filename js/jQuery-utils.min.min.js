'use strict';
$ (function () {
  $ ('a').click (function () {
    var s = $ ('p').css ('font-size'), i = parseFloat (s, 10);
    (s = s.slice (
      -2
    )), $ ('p').css ('font-size', i / 1.2 + s), 'larger' == this.id && $ ('p').css ('font-size', 1.2 * i + s);
  });
});
