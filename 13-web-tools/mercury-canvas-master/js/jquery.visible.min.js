'use strict';
var $jscompDefaultExport$$module$ = function ( b ) {
    var c = b( window );
    b.fn.visible = function () {
      if ( !( 1 > this.length ) ) {
        var a = ( 1 < this.length ? this.eq( 0 ) : this ).get( 0 ),
          d = c.width(),
          e = c.height();
        a = a.getBoundingClientRect();
        return {
          top: 0 > a.top ? -a.top : 0,
          bottom: a.bottom > e ? a.bottom - e : 0,
          left: 0 > a.left ? -a.left : 0,
          right: a.right > d ? a.right - d : 0
        }
      }
    }
  },
  module$ = {};
module$.default = $jscompDefaultExport$$module$;
