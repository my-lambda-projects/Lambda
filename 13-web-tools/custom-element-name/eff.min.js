'use strict';
( function ( b, e ) {
  function h( a ) {
    return encodeURIComponent( a ).replace( /['()_*]/g, function ( c ) {
      return "%" + c.charCodeAt().toString( 16 )
    } )
  }

  function k() {
    var a = d.value,
      c = validate( a );
    c.isValid ? ( validate( a ), l.className = "show", c.message ? ( d.className = "warning", f.innerHTML = "<em>However</em>, as a best practice, " + c.message, f.className = "show" ) : ( d.className = "valid", f.className = "hide" ) ) : ( f.innerHTML = c.message, f.className = "show", l.className = "hide", d.className = "invalid" );
    m.hash = h( a );
    g && ( g.customElementName =
      a )
  }
  var d = e.getElementsByTagName( "input" )[ 0 ],
    l = b.valid,
    f = b.message,
    m = e.getElementById( "permalink" ),
    g = function () {
      var a = new Date,
        c;
      try {
        ( c = b.localStorage ).setItem( a, a );
        var n = c.getItem( a ) == a;
        c.removeItem( a );
        return n && c
      } catch ( p ) {}
    }();
  d.onkeyup = k;
  d.oninput = function () {
    d.onkeyup = null;
    k()
  };
  g && ( g.customElementName && ( d.value = g.customElementName ), k() );
  b.onhashchange = function () {
    d.value = decodeURIComponent( location.hash.slice( 1 ) );
    k()
  };
  if ( location.hash ) b.onhashchange()
} )( this, document );
window._gaq = [
  [ "_setAccount", "UA-6065217-60" ],
  [ "_trackPageview" ]
];
( function ( b, e ) {
  var h = b.createElement( e );
  b = b.getElementsByTagName( e )[ 0 ];
  h.src = "https://www.google-analytics.com/ga.js";
  b.parentNode.insertBefore( h, b )
} )( document, "script" );
